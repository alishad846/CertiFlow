'use client';

// Locks down the editor UI (disables risky panels/actions) for restricted plans.
// Task 17 fills this in; for now it is a no-op so the editor pages have something to call.
export function useEditorLockdown() {}
